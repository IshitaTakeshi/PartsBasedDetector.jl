PartsBasedDetector
==================

#Setting the environment

```julia
Pkg.add("PartsBasedDetector")
cd deps
./build.sh
```

# Dependencies

Doxygen
OpenCV 2.4.9
Boost (apt-get install libboost-all-dev)
